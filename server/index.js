const Gd = require('./class');
const server = require('./app')('ws');

const port = 8080;
const host = '127.0.0.1';

const vector2 = Gd.vector2;
const rect2 = Gd.rect2;

server.start(port);













